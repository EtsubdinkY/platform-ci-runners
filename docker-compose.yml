services:
  platform-runner:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: platform-ci-runner

    env_file:
      - .env

    user: "runner"

    restart: unless-stopped